CREATE TABLE books
(
  genre     VARCHAR,
  author    VARCHAR,
  image     VARCHAR,
  title     VARCHAR,
  subtitle  VARCHAR,
  publisher VARCHAR,
  year      INT,
  pages     VARCHAR,
  isbn      VARCHAR
);
